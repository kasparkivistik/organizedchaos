export class Event {

  constructor(header, description, startDate, endDate) {
    this.name = header;
    this.description = description;
    this.startDate = startDate;
    this.endDate = endDate;
  }
}
